import React from 'react';
import './goal.css';

type GoalProps = {
    title: string;
    description: string;
}

const Goal = (props: GoalProps) => {
    return (
        <div className="goals-list">

        </div>
    );
};

export default Goal;